---
FuturePreparedStatement:
  comment: |-
    A future returned from `Session::prepareAsync()`
    This future will resolve with a PreparedStatement or an exception.

    @see \Cassandra\Session::prepareAsync()
  methods:
    get:
      comment: ""
      params:
        timeout:
          comment: ""
          type: int|double|null
      return:
        comment: A prepared statement
        type: \Cassandra\PreparedStatement
...
